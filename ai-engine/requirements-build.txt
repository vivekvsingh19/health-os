# Lightweight CI build requirements - only what's needed to RUN the posture detection
# MediaPipe installed without its optional pytorch/jax model_maker training deps
flask>=3.0.0
flask-cors>=4.0.0
opencv-python-headless>=4.9.0
numpy>=1.24.0
protobuf<4
absl-py>=1.0.0
attrs>=21.3.0
matplotlib>=3.3.0
# Install mediapipe without dependencies (avoids pulling in pytorch/CUDA/jax)
# The --no-deps flag is passed in the workflow
